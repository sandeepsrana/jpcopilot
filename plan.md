# ポモドーロタイマー 段階的実装計画

このドキュメントは、添付モック、アーキテクチャ方針、機能一覧を踏まえた段階的な実装計画を定義する。

## 1. 進め方の原則

- 機能は「モジュール単位」ではなく「ユーザーが触って価値を確認できる体験単位」で実装する
- 1段階の作業量は半日から1.5日程度を目安にする
- 各段階で受け入れ条件を明確にし、完了判定可能にする
- 未確定要素は明示的に次段階へ先送りする

## 2. Phase別実装計画

### Phase 0: 土台構築

目的:
- Flaskで画面を配信できる最小構成を作る

実装内容:
- Flaskアプリ起動エントリ
- ルート `GET /`
- テンプレート/静的ファイル配信の骨組み

完了条件:
- ブラウザでトップ画面が表示できる

先送り:
- タイマーの動作ロジック全般

### Phase 1: UIモック再現（静的）

目的:
- 添付モックの見た目を先に確定する

実装内容:
- タイトル、フェーズ表示、残り時間表示
- 円形リング（静的）
- 開始ボタン、リセットボタン
- 今日の進捗カード

完了条件:
- レイアウトがモックと大筋一致する
- 主要コンポーネント配置が崩れない

先送り:
- カウントダウン挙動

### Phase 2: タイマー最小動作

目的:
- 作業フェーズのみで開始/停止/リセットを成立させる

実装内容:
- TimerEngine最小版
- mm:ss更新
- ボタン状態切替（開始/停止）

完了条件:
- 25:00から動作し、停止/再開/リセットが可能

先送り:
- 休憩遷移
- 統計更新

### Phase 3: 高精度化 + 進捗リング連動

目的:
- 時間ずれを抑え、視覚進捗と同期させる

実装内容:
- 終了予定時刻との差分で残り時間算出
- SVGリングの進捗更新（stroke-dashoffset）

完了条件:
- タブ切替後の時間ずれが最小化される
- 時刻表示とリング進捗が一致する

先送り:
- フェーズ遷移

### Phase 4: フェーズ遷移ロジック

目的:
- focus/shortBreak/longBreakのサイクルを成立させる

実装内容:
- cycleCount管理
- 長休憩周期判定（例: 4セット）
- フェーズ完了時の自動遷移

完了条件:
- 作業完了後に短休憩/長休憩へ遷移する
- 休憩完了後に作業へ復帰する

先送り:
- 永続化

### Phase 5: 今日の進捗（メモリ上）

目的:
- 完了数と集中時間を正しく可視化する

実装内容:
- 完了ポモドーロ数の加算
- 集中時間合計の加算と表示（時:分）
- 作業フェーズ完了時のみ集中時間を加算

完了条件:
- 今日の進捗カードが操作に連動して更新される

先送り:
- 永続化

### Phase 6: LocalStorage永続化

目的:
- 再読み込み後も設定と当日統計を維持する

実装内容:
- 設定値保存/復元
- 当日統計保存/復元
- dateKey比較による日次リセット

完了条件:
- リロード後に状態復元できる
- 日付変更で統計が初期化される

先送り:
- サーバー保存

### Phase 7: 品質仕上げ

目的:
- 実運用前の使いやすさと品質を担保する

実装内容:
- レスポンシブ調整
- アクセシビリティ改善
- 軽微なUI/UX改善

完了条件:
- モバイル/デスクトップで可読性を維持
- 色以外でも状態判別可能

先送り:
- API実装

### Phase 8: 将来拡張準備

目的:
- サーバー保存への移行を容易にする

実装内容:
- APIエンドポイント仕様のスタブ整理
- Storage差し替え前提のインターフェース整理

完了条件:
- API移行時の改修対象が明確化されている

## 3. タスク粒度ルール

- 1タスクは2時間から4時間以内を目安にする
- 1タスクは「1責務 + 1検証」に限定する
- 1段階あたり3から6タスクに収める
- 受け入れ条件は手操作で確認できる文にする

## 4. 各段階のレビュー観点

- 挙動: 状態遷移が設計どおりか
- 見た目: モックとの乖離が許容範囲か
- 耐性: タブ切替、再読み込み、日跨ぎで破綻しないか
- 拡張性: 次段階で大改修が不要か
